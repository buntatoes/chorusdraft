# ChorusDraft 0.51.2 Ruby testing notes

This branch is an unreleased Ruby maintenance branch based on ChorusDraft 0.51.1.
No user-visible 0.51.2 changes have been committed yet.

## Current status

- The source and `VERSION` file remain at 0.51.1 until an actual 0.51.2 change is
  implemented and verified.
- There is no `v0.51.2` tag, GitHub release, or set of 0.51.2 archives.
- `main-ruby` contains the current supported Ruby release line.
- `elixir-experimental` is a separate unfinished rewrite and is not included in
  this branch or any Ruby release.

For an installable build, use the signed-off assets and `SHA256SUMS` from the
[0.51.1 GitHub release](https://github.com/buntatoes/chorusdraft/releases/tag/v0.51.1).
Its release notes describe the shipped behavior, security changes, requirements,
and compatibility details.

Future entries in this file must describe verified 0.51.2 behavior. Branch plans
or unfinished experiments should not be presented as released features.
